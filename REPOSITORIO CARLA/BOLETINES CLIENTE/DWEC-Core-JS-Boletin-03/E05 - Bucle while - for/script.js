
for (let i = 1; i <= 300; i++) {
    // código
}

//Inicializamos el bucle
let i = 1;
//Establecemos donde se para
while ( i <= 300 ) {
    // código
    i++; //Fijamos en cuanto se incrementa en cada vuelta
}